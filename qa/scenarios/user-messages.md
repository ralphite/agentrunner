# 模拟用户消息库(QA 负载,2026-07-19 草案待审)

**用途**:以真实用户口吻驱动会话,生成覆盖主要功能面的真实状态与
数据,供 webui/runtime QA 审查。每个 session 一个主题,消息按顺序
发送(部分标注了发送时机:如"上一条运行中发"= 测 queue/steer)。
**未经用户审定前不投产。**

---

## S1 · 修 bug 的日常(read/grep/edit/bash、diff 审阅、commit)

1. 帮我看一下这个项目里所有 TODO 注释,列出来,按文件分组
2. 第三个那个 TODO,就是 retry 那里,帮我实现了吧。注意别改公共接口
3. 跑一下测试看看有没有挂的
4. 有两个挂了是吧,修一下,修完把所有改动 commit,消息写清楚为什么这么改
5. 等等,你刚才是不是把 config.py 也动了?我没让你动这个文件,把它恢复,其他保留

## S2 · 从零起项目(写盘、多文件、markdown/表格/代码块输出)

1. 我们写一个命令行的番茄钟工具吧,Python 的,要能自定义时长、有声音提醒、统计每天完成了几个番茄
2. 给我一个使用说明,用表格列一下所有命令和参数
3. 加一个功能:每完成 4 个番茄自动提示休息 15 分钟。顺便把代码里的魔法数字都提出来做成配置
4. 你写的这个统计不对,我昨天的记录今天就看不到了?数据要落盘,存 JSON 就行
5. 行了,打个 tag v0.1,把 README 补一下

## S3 · 审批流(ask 模式:bash/网络逐条批,approve/deny 混合)

1. (ask 模式开会话)帮我查一下这台机器上 8000-9000 端口哪些被占了
2. 把占 8788 的那个进程 kill 掉 —— (预期弹审批,用户会 Deny 一次再改主意)
3. 算了别 kill,先告诉我它是谁起的、跑了多久
4. 下载 https://example.com/data.csv 到 /tmp 分析一下前十行 —— (网络审批)
5. 好,现在把分析结果写进 report.md,这个不用问我,直接写

## S4 · 多 agent 团队(spawn、blackboard、成员状态、子会话)

1. 我们组一个三人小组:一个负责设计 API、一个写实现、一个写测试,做一个短链接服务。你当 lead 协调,别自己动手写
2. 设计的那个进度怎么样了?让他先把接口定义发出来给另外两个
3. 测试那个同学好像卡住了,你去看看他在干嘛
4. 实现和测试有冲突的话让他们自己在黑板上讨论,你只汇总结论给我
5. 收工,让每个人交一份自己那部分的总结,你合并成一个最终报告

## S5 · goal 模式(work until verified、进度、暂停恢复)

1. (goal)把这个仓库的测试覆盖率提到 80% 以上,verified 之后才算完
2. 现在到多少了?哪几个模块还差?
3. 先暂停一下,别烧 token 了,我看看你已经写的这些测试
4. 继续吧,但 utils 那个模块不用管,把它排除在目标外
5. 可以了,就到这吧,把最终覆盖率数字和新增测试列表给我

## S6 · 长对话上下文管理(compact/clear、长贴、续聊)

1. (贴一段 ~200 行的报错日志)这是什么问题?
2. 按你说的改了还是报,这次的栈不一样 (再贴一段长日志)
3. 你上下文是不是已经很长了,压缩一下,重点保留我们对这个 bug 的结论
4. 好,现在从头梳理:这个 bug 的根因、我们试过什么、最终方案,写成 postmortem.md
5. clear 掉吧,开始下一个话题:这个项目怎么加国际化?

## S7 · 运行中交互(queue/steer、interrupt、改需求)

1. 把 src/ 下所有文件的版权头更新成 2026,一个一个来,每改 10 个汇报一次
2. (上一条运行中发,queue)改完之后顺便把行尾空格也清了
3. (运行中发,steer)等一下,third_party/ 目录跳过,那不是我们的代码
4. (interrupt 后)先停,我改主意了:只改 src/core/,其他不动
5. 重新跑吧

## S8 · 附件与多模态(图片、PDF、听写误差文本)

1. (贴一张 UI 截图)这个页面的布局问题你能看出来几个?
2. (贴 PDF)这是我们的 API 文档,按它实现一个 Python client,只要 auth 和 list 两个接口
3. 帮我把刚才截图里的错误信息原样敲出来,我要去搜
4. (口语化含错字)那个 clint 的重试逻辑加一下,指数退避,最多试 5 次
5. 对比一下你实现的和 PDF 里第 3 节写的,有没有出入

## S9 · worktree/分支/回滚(fork、checkpoint、Undo、apply back)

1. 在一个新 worktree 里做:把项目从 requirements.txt 迁移到 pyproject.toml
2. 迁完跑一遍安装验证,没问题的话告诉我改了哪些文件,先别合回去
3. 打个 checkpoint,我要试个激进的:把构建系统也换成 uv
4. 不行,uv 这条路有问题,回到刚才的 checkpoint
5. 就用 pyproject 那版,合回主工作区,worktree 清掉

## S10 · 定时与后台(schedule、监控、hook 唤醒)

1. 给这个会话挂一个每 30 分钟的定时:检查 /var/log/app.log 有没有新的 ERROR,有就总结给我
2. 太频繁了,改成每 2 小时,而且晚上 11 点到早 8 点不用跑
3. 刚才那次触发它说日志文件不存在?你处理一下这种情况,文件没有就静默跳过
4. 暂停这个定时,我们先聊别的
5. 这个定时干脆取消吧,换成:CI 失败的时候通过 webhook 叫醒你来分析

## S11 · 追问与纠错(答案质量、事实声明对账、中英混合)

1. What changes did you make in this workspace? 列全,一个都别漏
2. 你说改了 4 个文件,但我在 diff 里只看到 3 个,那个 __init__.py 呢?
3. Undo 掉最后一次改动,然后告诉我现在 workspace 是不是干净的
4. commit 历史里最近三条都是谁提交的?show me the exact hashes
5. 现在这个会话用了多少 token 了?比上个会话多还是少?

## S12 · 边界与容错(超长输入、特殊字符、错误恢复)

1. (一条 5000 字的需求文档粘贴)先别动手,把这个需求拆成任务清单,标出哪些有歧义
2. 文件名里带空格和中文的也要处理:帮我把 "设计 稿 final(2).md" 重命名成 design-final.md
3. `rm -rf` 这种命令你会直接执行吗?你的权限策略是什么,解释给我听
4. (制造 rate limit 后)刚才那次失败了,重试,这次一次做完别分批
5. 假如我现在关掉浏览器,你还在跑的任务会怎么样?测试一下:开始一个 5 分钟的任务,我过会儿回来看

---

**覆盖面对照**(JOURNEYS 主线):基础对话/续聊(全部)、工具链
read/grep/edit/bash(S1/S2)、审批 approve/deny/网络(S3)、多 agent
spawn/blackboard/子会话(S4)、goal/verified/暂停(S5)、compact/clear/
长贴(S6)、queue/steer/interrupt(S7)、图片/PDF/听写(S8)、worktree/
checkpoint/fork/回滚(S9)、schedule/cron/hook(S10)、事实声明对账/
diff/Undo/commit(S11)、超长输入/特殊字符/权限说明/容错/离线续跑
(S12)。未覆盖待补:自定义 persona/agent 切换、/mode 运行中切换、
dictate 真音频、best-of-N。
