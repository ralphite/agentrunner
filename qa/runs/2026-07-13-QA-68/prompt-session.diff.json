{"scope":"last-turn","available":true,"workspace":"/Users/yadong/dev2/agentrunner/webui/runtime/ws/ws-20260713-012616","input_seq":2,"barrier_seq":4,"barrier_id":"bar-t1","diff":"diff --git a/agent_runtime/README.md b/agent_runtime/README.md\nnew file mode 100644\nindex 0000000..55bbe1a\n--- /dev/null\n+++ b/agent_runtime/README.md\n@@ -0,0 +1,59 @@\n+# Agent Runtime 设计文档\n+\n+本项目实现了一个轻量级但功能完备的智能体运行时环境（Agent Runtime），重点支持 **Session State（会话状态持久化）** 和 **Transfer to Agent（智能体之间的控制权转移/委托）** 等核心工程特性。\n+\n+## 1. 核心架构设计\n+\n+运行时由以下几个核心模块组成：\n+\n+```\n+┌────────────────────────────────────────────────────────┐\n+│                      AgentRuntime                      │\n+│                                                        │\n+│  ┌──────────────────┐  ┌────────────────────────────┐  │\n+│  │   SessionManager │  │       AgentRegistry        │  │\n+│  │                  │  │                            │  │\n+│  │ ┌──────────────┐ │  │ ┌────────┐      ┌────────┐ │  │\n+│  │ │ SessionState │ │  │ │ AgentA │ ◄───► │ AgentB │ │  │\n+│  │ └──────────────┘ │  │ └────────┘      └────────┘ │  │\n+│  └──────────────────┘  └────────────────────────────┘  │\n+│                                                        │\n+│  ┌──────────────────────────────────────────────────┐  │\n+│  │                   ToolRegistry                   │  │\n+│  └──────────────────────────────────────────────────┘  │\n+└────────────────────────────────────────────────────────┘\n+```\n+\n+1. **SessionState & SessionManager**\n+   - 维护一个会话的状态，包括对话历史（`messages`）、键值对形式的上下文状态（`context_data`）、当前处于激活状态的智能体名称（`current_agent`）。\n+   - 支持将会话序列化为 JSON 格式并进行本地持久化，或从持久化文件中恢复。\n+\n+2. **Agent & AgentRegistry**\n+   - **Agent** 定义了智能体的元数据（`name`、`system_instruction`、绑定的 `tools`、所使用的 `model` 等）。\n+   - **AgentRegistry** 负责注册和管理所有可用的智能体。\n+   - **Transfer to Agent**: 智能体可通过调用内置/自定义的特殊工具，实现控制权的顺畅流转。例如调用 `transfer_to(target_agent_name)` 工具，运行时会自动修改当前 Session 的 `current_agent`，并由新智能体接管后续对话。\n+\n+3. **Tool & ToolRegistry**\n+   - 支持自定义工具的注册，工具可由普通 Python 函数定义，支持参数类型提示与 docstring 自动解析。\n+   - 自动生成符合大语言模型 API（如 OpenAI Function Calling / Tool Call 格式）的 JSON 架构描述。\n+\n+4. **Runtime Executor**\n+   - 负责核心的 Loop 执行过程：接收输入 -> 加载/获取 Session -> 递交到当前 Agent -> 调用 LLM（支持 Mock & 真实适配器） -> 处理 Tool Calls（包括特殊的 `transfer_to` 工具） -> 更新 Session State -> 返回响应。\n+\n+## 2. 目录结构设计\n+\n+```\n+agent_runtime/\n+├── __init__.py\n+├── session.py       # SessionState 与 SessionManager\n+├── agent.py         # Agent 定义与 AgentRegistry\n+├── tool.py          # 工具装饰器与 ToolRegistry\n+├── runtime.py       # 核心运行循环（Runtime）与执行逻辑\n+├── mock_llm.py      # LLM 模拟器（用于离线测试和演示）\n+└── tests/\n+    ├── __init__.py\n+    ├── test_session.py\n+    ├── test_tool.py\n+    ├── test_transfer.py\n+    └── test_runtime.py\n+```\ndiff --git a/agent_runtime/__init__.py b/agent_runtime/__init__.py\nnew file mode 100644\nindex 0000000..f497f2b\n--- /dev/null\n+++ b/agent_runtime/__init__.py\n@@ -0,0 +1 @@\n+# 空的 __init__.py 标记为包\ndiff --git a/agent_runtime/__pycache__/__init__.cpython-314.pyc b/agent_runtime/__pycache__/__init__.cpython-314.pyc\nnew file mode 100644\nindex 0000000..688069b\nBinary files /dev/null and b/agent_runtime/__pycache__/__init__.cpython-314.pyc differ\ndiff --git a/agent_runtime/__pycache__/agent.cpython-314.pyc b/agent_runtime/__pycache__/agent.cpython-314.pyc\nnew file mode 100644\nindex 0000000..66e98b2\nBinary files /dev/null and b/agent_runtime/__pycache__/agent.cpython-314.pyc differ\ndiff --git a/agent_runtime/__pycache__/mock_llm.cpython-314.pyc b/agent_runtime/__pycache__/mock_llm.cpython-314.pyc\nnew file mode 100644\nindex 0000000..c4dfe2b\nBinary files /dev/null and b/agent_runtime/__pycache__/mock_llm.cpython-314.pyc differ\ndiff --git a/agent_runtime/__pycache__/runtime.cpython-314.pyc b/agent_runtime/__pycache__/runtime.cpython-314.pyc\nnew file mode 100644\nindex 0000000..b10430a\nBinary files /dev/null and b/agent_runtime/__pycache__/runtime.cpython-314.pyc differ\ndiff --git a/agent_runtime/__pycache__/session.cpython-314.pyc b/agent_runtime/__pycache__/session.cpython-314.pyc\nnew file mode 100644\nindex 0000000..fa82987\nBinary files /dev/null and b/agent_runtime/__pycache__/session.cpython-314.pyc differ\ndiff --git a/agent_runtime/__pycache__/tool.cpython-314.pyc b/agent_runtime/__pycache__/tool.cpython-314.pyc\nnew file mode 100644\nindex 0000000..ed819a4\nBinary files /dev/null and b/agent_runtime/__pycache__/tool.cpython-314.pyc differ\ndiff --git a/agent_runtime/agent.py b/agent_runtime/agent.py\nnew file mode 100644\nindex 0000000..27a972d\n--- /dev/null\n+++ b/agent_runtime/agent.py\n@@ -0,0 +1,34 @@\n+from typing import Any, Dict, List, Optional\n+from .tool import Tool\n+\n+class Agent:\n+    \"\"\"\n+    智能体（Agent）定义。\n+    \"\"\"\n+    def __init__(self, name: str, system_instruction: str, tools: Optional[List[Tool]] = None, model: str = \"default-model\"):\n+        self.name = name\n+        self.system_instruction = system_instruction\n+        self.tools = tools if tools is not None else []\n+        self.model = model\n+\n+    def get_tool_schemas(self) -> List[Dict[str, Any]]:\n+        return [tool.schema for tool in self.tools]\n+\n+\n+class AgentRegistry:\n+    \"\"\"\n+    智能体注册表，集中管理可用的 Agent。\n+    \"\"\"\n+    def __init__(self):\n+        self._agents: Dict[str, Agent] = {}\n+\n+    def register_agent(self, agent: Agent):\n+        if agent.name in self._agents:\n+            raise ValueError(f\"Agent with name '{agent.name}' already registered.\")\n+        self._agents[agent.name] = agent\n+\n+    def get_agent(self, name: str) -> Optional[Agent]:\n+        return self._agents.get(name)\n+\n+    def list_agents(self) -> List[Agent]:\n+        return list(self._agents.values())\ndiff --git a/agent_runtime/mock_llm.py b/agent_runtime/mock_llm.py\nnew file mode 100644\nindex 0000000..2bf74a4\n--- /dev/null\n+++ b/agent_runtime/mock_llm.py\n@@ -0,0 +1,107 @@\n+import json\n+from typing import Any, Dict, List, Optional\n+from .agent import Agent\n+\n+class MockLLMResponse:\n+    \"\"\"\n+    模拟大语言模型的响应对象，支持 tool_calls 和普通文本回复。\n+    \"\"\"\n+    def __init__(self, content: Optional[str] = None, tool_calls: Optional[List[Dict[str, Any]]] = None):\n+        self.content = content\n+        self.tool_calls = tool_calls or []\n+\n+\n+class MockLLM:\n+    \"\"\"\n+    一个用于离线测试和快速演示的 Mock LLM。\n+    通过一些规则和预设，能够根据消息历史与可用的 tools 决定是调用工具，还是返回普通文本回复。\n+    \"\"\"\n+    def __init__(self):\n+        # 预设的规则规则库：[关键词, 触发的 tool_calls 或文本回复]\n+        self.rules: List[Dict[str, Any]] = []\n+\n+    def add_rule(self, keyword: str, response: MockLLMResponse):\n+        self.rules.append({\n+            \"keyword\": keyword,\n+            \"response\": response\n+        })\n+\n+    def generate(self, system_instruction: str, messages: List[Dict[str, Any]], tools_schema: List[Dict[str, Any]]) -> MockLLMResponse:\n+        # 获取最新的消息（不论是 user, assistant, 还是 tool）\n+        last_message = \"\"\n+        if messages:\n+            # 优先从最后一条消息中检测内容\n+            last_msg_obj = messages[-1]\n+            if last_msg_obj.get(\"content\"):\n+                last_message = last_msg_obj[\"content\"]\n+            elif last_msg_obj.get(\"role\") == \"tool\":\n+                last_message = last_msg_obj.get(\"content\", \"\")\n+\n+        # 1. 优先匹配配置好的 Rules\n+        for rule in self.rules:\n+            if rule[\"keyword\"] in last_message:\n+                return rule[\"response\"]\n+\n+        # 1.5 如果在前面的历史消息中有 user 消息，也作为一个备用匹配\n+        user_msg = \"\"\n+        for msg in reversed(messages):\n+            if msg[\"role\"] == \"user\":\n+                user_msg = msg[\"content\"]\n+                break\n+        for rule in self.rules:\n+            if rule[\"keyword\"] in user_msg:\n+                # 确保避免在拿到 tool 结果后依然重复触发 tool call，所以加个判断：\n+                # 如果我们已经执行过该 tool 调用了（即 messages 里面最后一个是 tool 角色），就不用再次触发\n+                if messages[-1][\"role\"] == \"tool\" and rule[\"response\"].tool_calls:\n+                    continue\n+                return rule[\"response\"]\n+\n+        # 2. 自动检测特殊转移逻辑：\n+        # 如果提到转交给某个特定的智能体名字（例如 \"转交给 support\"、\"帮我转接 sales\"）\n+        for agent_schema in tools_schema:\n+            func_name = agent_schema[\"function\"][\"name\"]\n+            if func_name == \"transfer_to\":\n+                # 检查输入文本中是否有目标 agent 名字\n+                for target in [\"sales\", \"support\", \"tech_support\", \"billing\"]:\n+                    if f\"转接{target}\" in last_message or f\"转给{target}\" in last_message or f\"transfer to {target}\" in last_message:\n+                        return MockLLMResponse(\n+                            content=f\"正在为您转接到 {target} 智能体...\",\n+                            tool_calls=[{\n+                                \"id\": \"call_mock_transfer_123\",\n+                                \"type\": \"function\",\n+                                \"function\": {\n+                                    \"name\": \"transfer_to\",\n+                                    \"arguments\": json.dumps({\"target_agent\": target})\n+                                }\n+                            }]\n+                        )\n+\n+        # 3. 自动匹配工具名称\n+        for tool in tools_schema:\n+            t_name = tool[\"function\"][\"name\"]\n+            if t_name in last_message or t_name.replace(\"_\", \" \") in last_message:\n+                # 提取一些虚拟参数\n+                args = {}\n+                properties = tool[\"function\"][\"parameters\"].get(\"properties\", {})\n+                for prop_name in properties:\n+                    if \"amount\" in prop_name:\n+                        args[prop_name] = 100\n+                    elif \"name\" in prop_name:\n+                        args[prop_name] = \"Alice\"\n+                    else:\n+                        args[prop_name] = \"test_val\"\n+\n+                return MockLLMResponse(\n+                    content=None,\n+                    tool_calls=[{\n+                        \"id\": f\"call_{t_name}_999\",\n+                        \"type\": \"function\",\n+                        \"function\": {\n+                            \"name\": t_name,\n+                            \"arguments\": json.dumps(args)\n+                        }\n+                    }]\n+                )\n+\n+        # 4. 兜底回复\n+        return MockLLMResponse(content=f\"我是智能体。我已经收到您的消息：'{last_message}'。\")\ndiff --git a/agent_runtime/runtime.py b/agent_runtime/runtime.py\nnew file mode 100644\nindex 0000000..0b4ac61\n--- /dev/null\n+++ b/agent_runtime/runtime.py\n@@ -0,0 +1,159 @@\n+import json\n+import logging\n+from typing import Any, Dict, List, Optional\n+from .session import SessionState, SessionManager\n+from .agent import Agent, AgentRegistry\n+from .tool import Tool, ToolRegistry\n+from .mock_llm import MockLLM, MockLLMResponse\n+\n+logger = logging.getLogger(\"AgentRuntime\")\n+\n+class AgentRuntime:\n+    \"\"\"\n+    智能体运行时（Agent Runtime），集成和协调 Session、Agents、Tools 以及 LLM 之间的生命周期与调用循环。\n+    \"\"\"\n+    def __init__(self, agent_registry: AgentRegistry, session_manager: SessionManager, llm_client: Optional[MockLLM] = None):\n+        self.agent_registry = agent_registry\n+        self.session_manager = session_manager\n+        self.llm_client = llm_client or MockLLM()\n+        self._global_tools = ToolRegistry()\n+        self._setup_global_tools()\n+\n+    def _setup_global_tools(self):\n+        \"\"\"\n+        初始化全局公共工具，例如最关键的 transfer_to 工具。\n+        \"\"\"\n+        # 定义一个智能体转移的内置工具函数\n+        def transfer_to(target_agent: str, session: SessionState) -> str:\n+            \"\"\"\n+            将当前的对话转移控制权到另一个目标智能体（Agent）。\n+            \"\"\"\n+            agent = self.agent_registry.get_agent(target_agent)\n+            if not agent:\n+                return f\"转移失败：目标智能体 '{target_agent}' 不存在。\"\n+            session.current_agent = target_agent\n+            return f\"成功。已将当前对话控制权转交给 '{target_agent}'。\"\n+\n+        transfer_tool = Tool(transfer_to, name=\"transfer_to\", description=\"Transfer control to another specialized agent.\")\n+        self._global_tools.register_tool_instance(transfer_tool)\n+\n+    def process_message(self, session_id: str, user_message: str, default_agent: str) -> Dict[str, Any]:\n+        \"\"\"\n+        核心输入处理循环，对应 LLM ReAct 或 Tool Use 循环。\n+        支持 Session State 自动保存、Agent 动态转移和多轮 Tool 执行。\n+        \"\"\"\n+        # 1. 获取/加载会话\n+        session = self.session_manager.get_session(session_id, default_agent)\n+        \n+        # 2. 追加用户新输入\n+        session.add_message(role=\"user\", content=user_message)\n+\n+        max_steps = 5  # 限制单次请求内最大 Tool 调用回环次数，防止死循环\n+        step = 0\n+        final_response_text = \"\"\n+\n+        while step < max_steps:\n+            step += 1\n+            # 3. 确定当前处于激活状态的 Agent\n+            current_agent_name = session.current_agent\n+            agent = self.agent_registry.get_agent(current_agent_name)\n+            if not agent:\n+                raise ValueError(f\"Active agent '{current_agent_name}' not found in registry.\")\n+\n+            # 4. 构建完整的上下文：系统提示词 + 历史对话\n+            system_msg = {\"role\": \"system\", \"content\": agent.system_instruction}\n+            full_messages = [system_msg] + session.messages\n+\n+            # 5. 整合可用工具：Agent 私有工具 + 全局公共工具（如 transfer_to）\n+            available_tools: Dict[str, Tool] = {}\n+            for t in agent.tools:\n+                available_tools[t.name] = t\n+            for t in self._global_tools.list_tools():\n+                available_tools[t.name] = t\n+\n+            tools_schema = [t.schema for t in available_tools.values()]\n+\n+            # 6. 调用 LLM 产生预测\n+            response: MockLLMResponse = self.llm_client.generate(\n+                system_instruction=agent.system_instruction,\n+                messages=full_messages,\n+                tools_schema=tools_schema\n+            )\n+\n+            # 7. 处理 LLM 响应\n+            # 7.1 如果有文本输出，则记录到会话中\n+            if response.content:\n+                final_response_text = response.content\n+                # 如果没有 tool calls，这一轮就结束了，我们把 LLM 文本写进消息历史\n+                if not response.tool_calls:\n+                    session.add_message(role=\"assistant\", content=response.content)\n+                    break\n+\n+            # 7.2 如果有 Tool Calls，需要逐一执行并返回结果给 LLM\n+            if response.tool_calls:\n+                # 记录 LLM 的 tool call 请求到历史（为了保持 OpenAI/Chat 格式兼容）\n+                session.add_message(\n+                    role=\"assistant\", \n+                    content=response.content or \"\", \n+                    tool_calls=response.tool_calls\n+                )\n+\n+                for tool_call in response.tool_calls:\n+                    tc_id = tool_call.get(\"id\", \"call_id\")\n+                    func_info = tool_call.get(\"function\", {})\n+                    func_name = func_info.get(\"name\")\n+                    func_args_str = func_info.get(\"arguments\", \"{}\")\n+\n+                    try:\n+                        func_args = json.loads(func_args_str)\n+                    except Exception:\n+                        func_args = {}\n+\n+                    # 执行工具\n+                    tool = available_tools.get(func_name)\n+                    if not tool:\n+                        tool_result = f\"Error: Tool '{func_name}' not found.\"\n+                    else:\n+                        # 检查工具签名，注入 session 或 context（上下文）\n+                        try:\n+                            # 准备参数\n+                            actual_kwargs = {}\n+                            import inspect\n+                            sig = inspect.signature(tool.func)\n+                            for param in sig.parameters.values():\n+                                if param.name == \"session\":\n+                                    actual_kwargs[\"session\"] = session\n+                                elif param.name == \"context\":\n+                                    actual_kwargs[\"context\"] = session.context_data\n+                                elif param.name in func_args:\n+                                    actual_kwargs[param.name] = func_args[param.name]\n+                            \n+                            tool_result = tool.execute(**actual_kwargs)\n+                        except Exception as e:\n+                            tool_result = f\"Error executing tool: {str(e)}\"\n+\n+                    # 将工具执行结果追加到消息历史\n+                    session.add_message(\n+                        role=\"tool\", \n+                        name=func_name, \n+                        content=str(tool_result), \n+                        tool_call_id=tc_id\n+                    )\n+                \n+                # 继续循环，让 LLM 看看工具返回结果并进行下一步决策\n+                continue\n+            else:\n+                # 没有 tool calls 也拿到了最终文本回复\n+                break\n+        else:\n+            final_response_text = final_response_text or \"达到最大执行步骤数，处理中止。\"\n+\n+        # 8. 持久化保存最新的会话状态\n+        self.session_manager.save_session(session)\n+\n+        return {\n+            \"session_id\": session.session_id,\n+            \"current_agent\": session.current_agent,\n+            \"response\": final_response_text,\n+            \"context_data\": session.context_data\n+        }\ndiff --git a/agent_runtime/session.py b/agent_runtime/session.py\nnew file mode 100644\nindex 0000000..9a4992b\n--- /dev/null\n+++ b/agent_runtime/session.py\n@@ -0,0 +1,91 @@\n+import json\n+import os\n+from typing import Any, Dict, List, Optional\n+\n+class SessionState:\n+    \"\"\"\n+    表示会话的内存状态，包含对话历史、上下文数据以及当前负责处理请求的 Agent。\n+    \"\"\"\n+    def __init__(self, session_id: str, current_agent: str, context_data: Optional[Dict[str, Any]] = None, messages: Optional[List[Dict[str, Any]]] = None):\n+        self.session_id = session_id\n+        self.current_agent = current_agent\n+        self.context_data = context_data if context_data is not None else {}\n+        self.messages = messages if messages is not None else []\n+\n+    def add_message(self, role: str, content: str, name: Optional[str] = None, tool_calls: Optional[List[Dict[str, Any]]] = None, tool_call_id: Optional[str] = None):\n+        message = {\n+            \"role\": role,\n+            \"content\": content\n+        }\n+        if name:\n+            message[\"name\"] = name\n+        if tool_calls:\n+            message[\"tool_calls\"] = tool_calls\n+        if tool_call_id:\n+            message[\"tool_call_id\"] = tool_call_id\n+        self.messages.append(message)\n+\n+    def set_context(self, key: str, value: Any):\n+        self.context_data[key] = value\n+\n+    def get_context(self, key: str, default: Any = None) -> Any:\n+        return self.context_data.get(key, default)\n+\n+    def to_dict(self) -> Dict[str, Any]:\n+        return {\n+            \"session_id\": self.session_id,\n+            \"current_agent\": self.current_agent,\n+            \"context_data\": self.context_data,\n+            \"messages\": self.messages\n+        }\n+\n+    @classmethod\n+    def from_dict(cls, data: Dict[str, Any]) -> \"SessionState\":\n+        return cls(\n+            session_id=data[\"session_id\"],\n+            current_agent=data[\"current_agent\"],\n+            context_data=data.get(\"context_data\", {}),\n+            messages=data.get(\"messages\", [])\n+        )\n+\n+\n+class SessionManager:\n+    \"\"\"\n+    负责 SessionState 的生命周期管理，包括持久化和反序列化。\n+    \"\"\"\n+    def __init__(self, storage_dir: Optional[str] = None):\n+        self.storage_dir = storage_dir\n+        self._sessions: Dict[str, SessionState] = {}\n+        if storage_dir and not os.path.exists(storage_dir):\n+            os.makedirs(storage_dir)\n+\n+    def get_session(self, session_id: str, default_agent: str) -> SessionState:\n+        # 1. 尝试从内存读取\n+        if session_id in self._sessions:\n+            return self._sessions[session_id]\n+\n+        # 2. 尝试从磁盘加载\n+        if self.storage_dir:\n+            filepath = os.path.join(self.storage_dir, f\"{session_id}.json\")\n+            if os.path.exists(filepath):\n+                try:\n+                    with open(filepath, 'r', encoding='utf-8') as f:\n+                        data = json.load(f)\n+                        state = SessionState.from_dict(data)\n+                        self._sessions[session_id] = state\n+                        return state\n+                except Exception as e:\n+                    # 容错：加载失败则创建新 Session\n+                    pass\n+\n+        # 3. 创建全新 Session\n+        state = SessionState(session_id=session_id, current_agent=default_agent)\n+        self._sessions[session_id] = state\n+        return state\n+\n+    def save_session(self, state: SessionState):\n+        self._sessions[state.session_id] = state\n+        if self.storage_dir:\n+            filepath = os.path.join(self.storage_dir, f\"{state.session_id}.json\")\n+            with open(filepath, 'w', encoding='utf-8') as f:\n+                json.dump(state.to_dict(), f, indent=4, ensure_ascii=False)\ndiff --git a/agent_runtime/tests/__init__.py b/agent_runtime/tests/__init__.py\nnew file mode 100644\nindex 0000000..78186a9\n--- /dev/null\n+++ b/agent_runtime/tests/__init__.py\n@@ -0,0 +1 @@\n+# 空文件，支持 unittest 自动发现测试用例\ndiff --git a/agent_runtime/tests/__pycache__/test_runtime.cpython-314.pyc b/agent_runtime/tests/__pycache__/test_runtime.cpython-314.pyc\nnew file mode 100644\nindex 0000000..e104631\nBinary files /dev/null and b/agent_runtime/tests/__pycache__/test_runtime.cpython-314.pyc differ\ndiff --git a/agent_runtime/tests/__pycache__/test_session.cpython-314.pyc b/agent_runtime/tests/__pycache__/test_session.cpython-314.pyc\nnew file mode 100644\nindex 0000000..fb8bad3\nBinary files /dev/null and b/agent_runtime/tests/__pycache__/test_session.cpython-314.pyc differ\ndiff --git a/agent_runtime/tests/__pycache__/test_tool.cpython-314.pyc b/agent_runtime/tests/__pycache__/test_tool.cpython-314.pyc\nnew file mode 100644\nindex 0000000..2faacd5\nBinary files /dev/null and b/agent_runtime/tests/__pycache__/test_tool.cpython-314.pyc differ\ndiff --git a/agent_runtime/tests/__pycache__/test_transfer.cpython-314.pyc b/agent_runtime/tests/__pycache__/test_transfer.cpython-314.pyc\nnew file mode 100644\nindex 0000000..3dd27e6\nBinary files /dev/null and b/agent_runtime/tests/__pycache__/test_transfer.cpython-314.pyc differ\ndiff --git a/agent_runtime/tests/test_runtime.py b/agent_runtime/tests/test_runtime.py\nnew file mode 100644\nindex 0000000..55d73f2\n--- /dev/null\n+++ b/agent_runtime/tests/test_runtime.py\n@@ -0,0 +1,83 @@\n+import unittest\n+import json\n+import tempfile\n+import shutil\n+from agent_runtime.agent import Agent, AgentRegistry\n+from agent_runtime.session import SessionManager\n+from agent_runtime.tool import Tool\n+from agent_runtime.mock_llm import MockLLM, MockLLMResponse\n+from agent_runtime.runtime import AgentRuntime\n+\n+class TestRuntime(unittest.TestCase):\n+    def setUp(self):\n+        self.test_dir = tempfile.mkdtemp()\n+        self.session_manager = SessionManager(storage_dir=self.test_dir)\n+        self.agent_registry = AgentRegistry()\n+        self.mock_llm = MockLLM()\n+\n+    def tearDown(self):\n+        shutil.rmtree(self.test_dir)\n+\n+    def test_end_to_end_runtime_flow(self):\n+        # 1. 创建一些测试工具\n+        def query_bill(year: int, context: dict) -> str:\n+            \"\"\"Query bill of a specific year.\"\"\"\n+            return f\"Bill for year {year} is $500.\"\n+\n+        bill_tool = Tool(query_bill)\n+\n+        # 2. 注册 Agents\n+        billing_agent = Agent(\n+            name=\"billing_agent\",\n+            system_instruction=\"You help customers with bill problems.\",\n+            tools=[bill_tool]\n+        )\n+        self.agent_registry.register_agent(billing_agent)\n+\n+        # 3. 初始化 Runtime\n+        runtime = AgentRuntime(\n+            agent_registry=self.agent_registry,\n+            session_manager=self.session_manager,\n+            llm_client=self.mock_llm\n+        )\n+\n+        # 4. 配置 mock llm 规则：当用户说 \"查一下2025年账单\" 时调用 query_bill 工具\n+        self.mock_llm.add_rule(\n+            keyword=\"查一下2025年账单\",\n+            response=MockLLMResponse(\n+                content=None,\n+                tool_calls=[{\n+                    \"id\": \"call_bill_001\",\n+                    \"type\": \"function\",\n+                    \"function\": {\n+                        \"name\": \"query_bill\",\n+                        \"arguments\": json.dumps({\"year\": 2025})\n+                    }\n+                }]\n+            )\n+        )\n+        \n+        # 还要配置工具执行完后，LLM 总结结果并回复的 Mock 响应规则\n+        self.mock_llm.add_rule(\n+            keyword=\"Bill for year 2025 is $500.\",\n+            response=MockLLMResponse(content=\"我帮您查到了，您2025年的账单是 500 美元。\")\n+        )\n+\n+        # 5. 触发 Runtime 运行\n+        result = runtime.process_message(\n+            session_id=\"s_test_001\",\n+            user_message=\"你好，帮我查一下2025年账单，谢谢！\",\n+            default_agent=\"billing_agent\"\n+        )\n+\n+        # 验证返回内容\n+        self.assertEqual(result[\"session_id\"], \"s_test_001\")\n+        self.assertEqual(result[\"current_agent\"], \"billing_agent\")\n+        self.assertEqual(result[\"response\"], \"我帮您查到了，您2025年的账单是 500 美元。\")\n+\n+        # 验证 Session 数据持久化到了磁盘\n+        session_loaded = self.session_manager.get_session(\"s_test_001\", default_agent=\"billing_agent\")\n+        self.assertGreater(len(session_loaded.messages), 1)\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\ndiff --git a/agent_runtime/tests/test_session.py b/agent_runtime/tests/test_session.py\nnew file mode 100644\nindex 0000000..4398a4a\n--- /dev/null\n+++ b/agent_runtime/tests/test_session.py\n@@ -0,0 +1,40 @@\n+import unittest\n+from agent_runtime.session import SessionState, SessionManager\n+\n+class TestSession(unittest.TestCase):\n+    def test_session_state_creation(self):\n+        state = SessionState(session_id=\"session_123\", current_agent=\"receptionist\")\n+        self.assertEqual(state.session_id, \"session_123\")\n+        self.assertEqual(state.current_agent, \"receptionist\")\n+        self.assertEqual(state.context_data, {})\n+        self.assertEqual(state.messages, [])\n+\n+    def test_session_state_messages(self):\n+        state = SessionState(session_id=\"session_123\", current_agent=\"receptionist\")\n+        state.add_message(role=\"user\", content=\"hello\")\n+        state.add_message(role=\"assistant\", content=\"hi there\")\n+        \n+        self.assertEqual(len(state.messages), 2)\n+        self.assertEqual(state.messages[0][\"role\"], \"user\")\n+        self.assertEqual(state.messages[0][\"content\"], \"hello\")\n+\n+    def test_session_context(self):\n+        state = SessionState(session_id=\"session_123\", current_agent=\"receptionist\")\n+        state.set_context(\"username\", \"Alice\")\n+        self.assertEqual(state.get_context(\"username\"), \"Alice\")\n+        self.assertEqual(state.get_context(\"non_existent\", \"default_val\"), \"default_val\")\n+\n+    def test_session_serialization(self):\n+        state = SessionState(session_id=\"session_123\", current_agent=\"receptionist\")\n+        state.set_context(\"order_id\", 9999)\n+        state.add_message(role=\"user\", content=\"check order\")\n+        \n+        data_dict = state.to_dict()\n+        new_state = SessionState.from_dict(data_dict)\n+        \n+        self.assertEqual(new_state.session_id, \"session_123\")\n+        self.assertEqual(new_state.get_context(\"order_id\"), 9999)\n+        self.assertEqual(len(new_state.messages), 1)\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\ndiff --git a/agent_runtime/tests/test_tool.py b/agent_runtime/tests/test_tool.py\nnew file mode 100644\nindex 0000000..ab1073c\n--- /dev/null\n+++ b/agent_runtime/tests/test_tool.py\n@@ -0,0 +1,31 @@\n+import unittest\n+from agent_runtime.tool import Tool, ToolRegistry\n+\n+class TestTool(unittest.TestCase):\n+    def test_tool_schema_generation(self):\n+        def add_numbers(a: int, b: int) -> int:\n+            \"\"\"Add two numbers together.\"\"\"\n+            return a + b\n+\n+        tool = Tool(add_numbers)\n+        self.assertEqual(tool.name, \"add_numbers\")\n+        self.assertEqual(tool.description, \"Add two numbers together.\")\n+        \n+        schema = tool.schema\n+        self.assertEqual(schema[\"type\"], \"function\")\n+        self.assertEqual(schema[\"function\"][\"name\"], \"add_numbers\")\n+        properties = schema[\"function\"][\"parameters\"][\"properties\"]\n+        self.assertIn(\"a\", properties)\n+        self.assertIn(\"b\", properties)\n+        self.assertEqual(properties[\"a\"][\"type\"], \"integer\")\n+\n+    def test_tool_execution(self):\n+        def dummy_func(text: str):\n+            return f\"Processed: {text}\"\n+\n+        tool = Tool(dummy_func)\n+        result = tool.execute(text=\"hello\")\n+        self.assertEqual(result, \"Processed: hello\")\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\ndiff --git a/agent_runtime/tests/test_transfer.py b/agent_runtime/tests/test_transfer.py\nnew file mode 100644\nindex 0000000..3c651f6\n--- /dev/null\n+++ b/agent_runtime/tests/test_transfer.py\n@@ -0,0 +1,42 @@\n+import unittest\n+from agent_runtime.agent import Agent, AgentRegistry\n+from agent_runtime.session import SessionManager\n+from agent_runtime.runtime import AgentRuntime\n+from agent_runtime.tool import Tool\n+\n+class TestTransfer(unittest.TestCase):\n+    def test_transfer_between_agents(self):\n+        # 1. 创建 Registry 和 Manager\n+        agent_registry = AgentRegistry()\n+        session_manager = SessionManager()\n+\n+        # 2. 定义两个 Agents\n+        receptionist = Agent(\n+            name=\"receptionist\",\n+            system_instruction=\"You are a friendly receptionist. Transfer to 'tech_support' if technical help is needed.\"\n+        )\n+        tech_support = Agent(\n+            name=\"tech_support\",\n+            system_instruction=\"You are a senior technical support specialist. Help user solve code errors.\"\n+        )\n+\n+        agent_registry.register_agent(receptionist)\n+        agent_registry.register_agent(tech_support)\n+\n+        # 3. 创建 Runtime\n+        runtime = AgentRuntime(agent_registry=agent_registry, session_manager=session_manager)\n+\n+        # 4. 执行手动或自动 transfer_to 工具调用来测试状态流转\n+        session = session_manager.get_session(\"user_s1\", default_agent=\"receptionist\")\n+        self.assertEqual(session.current_agent, \"receptionist\")\n+\n+        # 我们通过 Runtime 预置的 transfer_to 工具，直接调用转到 tech_support\n+        transfer_tool = runtime._global_tools.get_tool(\"transfer_to\")\n+        self.assertIsNotNone(transfer_tool)\n+\n+        result = transfer_tool.execute(target_agent=\"tech_support\", session=session)\n+        self.assertIn(\"tech_support\", result)\n+        self.assertEqual(session.current_agent, \"tech_support\")\n+\n+if __name__ == \"__main__\":\n+    unittest.main()\ndiff --git a/agent_runtime/tool.py b/agent_runtime/tool.py\nnew file mode 100644\nindex 0000000..f22e082\n--- /dev/null\n+++ b/agent_runtime/tool.py\n@@ -0,0 +1,89 @@\n+import inspect\n+from typing import Any, Callable, Dict, List, Optional, get_type_hints\n+\n+class Tool:\n+    \"\"\"\n+    包装一个 Python 函数，使其可以作为智能体的工具，并自动提取参数结构。\n+    \"\"\"\n+    def __init__(self, func: Callable, name: Optional[str] = None, description: Optional[str] = None):\n+        self.func = func\n+        self.name = name or func.__name__\n+        self.description = description or func.__doc__ or f\"Execute tool {self.name}\"\n+        self._schema = self._generate_schema()\n+\n+    def _generate_schema(self) -> Dict[str, Any]:\n+        sig = inspect.signature(self.func)\n+        type_hints = get_type_hints(self.func)\n+        properties = {}\n+        required = []\n+\n+        for param_name, param in sig.parameters.items():\n+            if param_name in ('session', 'context'):  # 注入参数，不暴露给LLM\n+                continue\n+            \n+            param_type = type_hints.get(param_name, str)\n+            type_str = \"string\"\n+            if param_type == int:\n+                type_str = \"integer\"\n+            elif param_type == float:\n+                type_str = \"number\"\n+            elif param_type == bool:\n+                type_str = \"boolean\"\n+            elif param_type == dict:\n+                type_str = \"object\"\n+            elif param_type == list:\n+                type_str = \"array\"\n+\n+            properties[param_name] = {\n+                \"type\": type_str,\n+                \"description\": f\"Parameter {param_name}\"\n+            }\n+            if param.default == inspect.Parameter.empty:\n+                required.append(param_name)\n+\n+        return {\n+            \"type\": \"function\",\n+            \"function\": {\n+                \"name\": self.name,\n+                \"description\": self.description,\n+                \"parameters\": {\n+                    \"type\": \"object\",\n+                    \"properties\": properties,\n+                    \"required\": required\n+                }\n+            }\n+        }\n+\n+    @property\n+    def schema(self) -> Dict[str, Any]:\n+        return self._schema\n+\n+    def execute(self, *args, **kwargs) -> Any:\n+        return self.func(*args, **kwargs)\n+\n+\n+class ToolRegistry:\n+    \"\"\"\n+    工具注册中心\n+    \"\"\"\n+    def __init__(self):\n+        self._tools: Dict[str, Tool] = {}\n+\n+    def register(self, name: Optional[str] = None, description: Optional[str] = None):\n+        \"\"\"\n+        装饰器：注册普通 Python 函数为 Tool\n+        \"\"\"\n+        def decorator(func: Callable):\n+            tool = Tool(func, name=name, description=description)\n+            self._tools[tool.name] = tool\n+            return func\n+        return decorator\n+\n+    def register_tool_instance(self, tool: Tool):\n+        self._tools[tool.name] = tool\n+\n+    def get_tool(self, name: str) -> Optional[Tool]:\n+        return self._tools.get(name)\n+\n+    def list_tools(self) -> List[Tool]:\n+        return list(self._tools.values())\ndiff --git a/demo.py b/demo.py\nnew file mode 100644\nindex 0000000..6230080\n--- /dev/null\n+++ b/demo.py\n@@ -0,0 +1,132 @@\n+import os\n+from agent_runtime.session import SessionManager\n+from agent_runtime.agent import Agent, AgentRegistry\n+from agent_runtime.tool import Tool\n+from agent_runtime.runtime import AgentRuntime\n+from agent_runtime.mock_llm import MockLLM, MockLLMResponse\n+\n+def build_demo_system() -> AgentRuntime:\n+    # 1. 注册基础工具\n+    def query_user_profile(user_id: str) -> str:\n+        \"\"\"Query basic profile information of a user.\"\"\"\n+        return f\"User {user_id}: Name=Alice, MemberLevel=Gold\"\n+\n+    def process_refund(order_id: str, amount: float) -> str:\n+        \"\"\"Process refund for a given order amount.\"\"\"\n+        return f\"Refund of ${amount} for Order {order_id} has been processed successfully.\"\n+\n+    # 2. 注册智能体 (Agents)\n+    # 2.1 接待/分发智能体 (receptionist)\n+    reception_agent = Agent(\n+        name=\"receptionist\",\n+        system_instruction=\"You are receptionist. You greet users and direct them to specialized agents: 'billing_agent' or 'support_agent'.\"\n+    )\n+    \n+    # 2.2 账单智能体 (billing_agent)\n+    billing_agent = Agent(\n+        name=\"billing_agent\",\n+        system_instruction=\"You are a specialized billing assistant. You can query profile or process refunds.\",\n+        tools=[Tool(query_user_profile), Tool(process_refund)]\n+    )\n+\n+    # 2.3 技术支持智能体 (support_agent)\n+    support_agent = Agent(\n+        name=\"support_agent\",\n+        system_instruction=\"You are a senior tech support. You answer technical questions with high quality.\"\n+    )\n+\n+    # 3. 注册到注册表\n+    registry = AgentRegistry()\n+    registry.register_agent(reception_agent)\n+    registry.register_agent(billing_agent)\n+    registry.register_agent(support_agent)\n+\n+    # 4. 创建 Session 管理器\n+    session_manager = SessionManager(storage_dir=\"demo_sessions\")\n+\n+    # 5. 实例化并配置 MockLLM 使得交互变得智能化\n+    mock_llm = MockLLM()\n+    # 5.1 配置接待转移规则\n+    mock_llm.add_rule(\n+        \"我要退款\",\n+        MockLLMResponse(\n+            content=\"正在为您转接到 账单智能体 处理退款...\",\n+            tool_calls=[{\n+                \"id\": \"call_transfer_billing\",\n+                \"type\": \"function\",\n+                \"function\": {\n+                    \"name\": \"transfer_to\",\n+                    \"arguments\": '{\"target_agent\": \"billing_agent\"}'\n+                }\n+            }]\n+        )\n+    )\n+    mock_llm.add_rule(\n+        \"技术问题\",\n+        MockLLMResponse(\n+            content=\"正在为您转接到 技术支持 智能体...\",\n+            tool_calls=[{\n+                \"id\": \"call_transfer_support\",\n+                \"type\": \"function\",\n+                \"function\": {\n+                    \"name\": \"transfer_to\",\n+                    \"arguments\": '{\"target_agent\": \"support_agent\"}'\n+                }\n+            }]\n+        )\n+    )\n+\n+    # 5.2 配置退款具体规则\n+    mock_llm.add_rule(\n+        \"申请退100元\",\n+        MockLLMResponse(\n+            content=None,\n+            tool_calls=[{\n+                \"id\": \"call_refund_op\",\n+                \"type\": \"function\",\n+                \"function\": {\n+                    \"name\": \"process_refund\",\n+                    \"arguments\": '{\"order_id\": \"ORD9988\", \"amount\": 100.0}'\n+                }\n+            }]\n+        )\n+    )\n+    mock_llm.add_rule(\n+        \"Refund of $100.0 for Order ORD9988 has been processed successfully.\",\n+        MockLLMResponse(content=\"我已为您成功办理 100 元退款！退款单号为 ORD9988。\")\n+    )\n+\n+    # 6. 初始化运行时\n+    runtime = AgentRuntime(agent_registry=registry, session_manager=session_manager, llm_client=mock_llm)\n+    return runtime\n+\n+def run_demo():\n+    print(\"==================================================\")\n+    print(\"   欢迎使用智能体运行时（Agent Runtime）交互 Demo\")\n+    print(\"==================================================\")\n+    print(\"系统已就绪！初始化默认智能体: receptionist\")\n+    print(\"支持的核心特性: \\n1. Session State (会话状态自动记忆与持久化)\\n2. Transfer to Agent (多智能体无缝路由/接管)\")\n+    print(\"--------------------------------------------------\")\n+\n+    runtime = build_demo_system()\n+    session_id = \"demo_user_007\"\n+\n+    # 清除旧的 Demo session 文件（若存在）\n+    if os.path.exists(\"demo_sessions/demo_user_007.json\"):\n+        os.remove(\"demo_sessions/demo_user_007.json\")\n+\n+    # 交互 1\n+    print(\"\\n[User]: 你好，我有一个退款问题需要处理，我要退款。\")\n+    res1 = runtime.process_message(session_id, \"你好，我有一个退款问题需要处理，我要退款。\", default_agent=\"receptionist\")\n+    print(f\"[Agent ({res1['current_agent']})]: {res1['response']}\")\n+\n+    # 交互 2 (自动转移后，由 billing_agent 承接上下文)\n+    print(\"\\n[User]: 好的，我想申请退100元。\")\n+    res2 = runtime.process_message(session_id, \"好的，我想申请退100元。\", default_agent=\"receptionist\")\n+    print(f\"[Agent ({res2['current_agent']})]: {res2['response']}\")\n+\n+    print(\"\\n[系统提示] 会话数据已自动序列化并存储至 demo_sessions/ 文件夹下。\")\n+    print(\"==================================================\")\n+\n+if __name__ == \"__main__\":\n+    run_demo()\ndiff --git a/demo_sessions/demo_user_007.json b/demo_sessions/demo_user_007.json\nnew file mode 100644\nindex 0000000..d88babb\n--- /dev/null\n+++ b/demo_sessions/demo_user_007.json\n@@ -0,0 +1,63 @@\n+{\n+    \"session_id\": \"demo_user_007\",\n+    \"current_agent\": \"billing_agent\",\n+    \"context_data\": {},\n+    \"messages\": [\n+        {\n+            \"role\": \"user\",\n+            \"content\": \"你好，我有一个退款问题需要处理，我要退款。\"\n+        },\n+        {\n+            \"role\": \"assistant\",\n+            \"content\": \"正在为您转接到 账单智能体 处理退款...\",\n+            \"tool_calls\": [\n+                {\n+                    \"id\": \"call_transfer_billing\",\n+                    \"type\": \"function\",\n+                    \"function\": {\n+                        \"name\": \"transfer_to\",\n+                        \"arguments\": \"{\\\"target_agent\\\": \\\"billing_agent\\\"}\"\n+                    }\n+                }\n+            ]\n+        },\n+        {\n+            \"role\": \"tool\",\n+            \"content\": \"成功。已将当前对话控制权转交给 'billing_agent'。\",\n+            \"name\": \"transfer_to\",\n+            \"tool_call_id\": \"call_transfer_billing\"\n+        },\n+        {\n+            \"role\": \"assistant\",\n+            \"content\": \"我是智能体。我已经收到您的消息：'成功。已将当前对话控制权转交给 'billing_agent'。'。\"\n+        },\n+        {\n+            \"role\": \"user\",\n+            \"content\": \"好的，我想申请退100元。\"\n+        },\n+        {\n+            \"role\": \"assistant\",\n+            \"content\": \"\",\n+            \"tool_calls\": [\n+                {\n+                    \"id\": \"call_refund_op\",\n+                    \"type\": \"function\",\n+                    \"function\": {\n+                        \"name\": \"process_refund\",\n+                        \"arguments\": \"{\\\"order_id\\\": \\\"ORD9988\\\", \\\"amount\\\": 100.0}\"\n+                    }\n+                }\n+            ]\n+        },\n+        {\n+            \"role\": \"tool\",\n+            \"content\": \"Refund of $100.0 for Order ORD9988 has been processed successfully.\",\n+            \"name\": \"process_refund\",\n+            \"tool_call_id\": \"call_refund_op\"\n+        },\n+        {\n+            \"role\": \"assistant\",\n+            \"content\": \"我已为您成功办理 100 元退款！退款单号为 ORD9988。\"\n+        }\n+    ]\n+}\n\\ No newline at end of file","numstat":"59\t0\tagent_runtime/README.md\n1\t0\tagent_runtime/__init__.py\n-\t-\tagent_runtime/__pycache__/__init__.cpython-314.pyc\n-\t-\tagent_runtime/__pycache__/agent.cpython-314.pyc\n-\t-\tagent_runtime/__pycache__/mock_llm.cpython-314.pyc\n-\t-\tagent_runtime/__pycache__/runtime.cpython-314.pyc\n-\t-\tagent_runtime/__pycache__/session.cpython-314.pyc\n-\t-\tagent_runtime/__pycache__/tool.cpython-314.pyc\n34\t0\tagent_runtime/agent.py\n107\t0\tagent_runtime/mock_llm.py\n159\t0\tagent_runtime/runtime.py\n91\t0\tagent_runtime/session.py\n1\t0\tagent_runtime/tests/__init__.py\n-\t-\tagent_runtime/tests/__pycache__/test_runtime.cpython-314.pyc\n-\t-\tagent_runtime/tests/__pycache__/test_session.cpython-314.pyc\n-\t-\tagent_runtime/tests/__pycache__/test_tool.cpython-314.pyc\n-\t-\tagent_runtime/tests/__pycache__/test_transfer.cpython-314.pyc\n83\t0\tagent_runtime/tests/test_runtime.py\n40\t0\tagent_runtime/tests/test_session.py\n31\t0\tagent_runtime/tests/test_tool.py\n42\t0\tagent_runtime/tests/test_transfer.py\n89\t0\tagent_runtime/tool.py\n132\t0\tdemo.py\n63\t0\tdemo_sessions/demo_user_007.json"}
