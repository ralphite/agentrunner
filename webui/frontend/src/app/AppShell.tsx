// Canonical import path for the production layout. The implementation remains
// in App.tsx while INC-99 extracts leaf views, so the shell can move without a
// flag-day rewrite of its existing tests and styles.
export { AppShell } from "../App";
