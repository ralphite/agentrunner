{
  "name": "exit_plan_mode",
  "description": "Request to leave plan mode and begin making changes. Requires user approval. Call this when your plan is ready to execute.",
  "class": "wait",
  "input_schema": {
    "type": "object",
    "properties": {
      "plan": {
        "type": "string",
        "description": "A short summary of the plan you intend to execute."
      }
    }
  }
}
