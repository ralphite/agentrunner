{
  "name": "task_output",
  "description": "Check a background task's status. Returns running/unknown; a finished task's result arrives as a message instead.",
  "class": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "task_id": {
        "type": "string",
        "description": "The task handle returned when the background command started."
      }
    },
    "required": ["task_id"]
  }
}
