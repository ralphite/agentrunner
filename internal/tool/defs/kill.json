{
  "name": "kill",
  "description": "Cancel a running background task or sub-agent by handle. A replaced or no-longer-needed sub-agent keeps running — and keeps spending the shared budget — until it is killed, so kill it as soon as you stop needing it. Its cancellation notice arrives as a message.",
  "class": "execute",
  "input_schema": {
    "type": "object",
    "properties": {
      "handle": {
        "type": "string",
        "description": "The task handle to cancel."
      }
    },
    "required": ["handle"]
  }
}
