{
  "name": "output",
  "description": "Check background work progress. For a running work it returns status plus the tail of its stdout/stderr so far (output_tail, bounded); the full result still arrives as a message when it finishes.",
  "class": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "handle": {
        "type": "string",
        "description": "The work handle returned when the background command started."
      }
    },
    "required": ["handle"]
  }
}
