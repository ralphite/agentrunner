{
  "name": "output",
  "description": "Check background work status. Returns running/unknown; a finished result arrives as a message instead.",
  "class": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "handle": {
        "type": "string",
        "description": "The work handle returned when the background command started."
      }
    },
    "required": ["handle"]
  }
}
