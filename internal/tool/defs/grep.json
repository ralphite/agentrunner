{
  "name": "grep",
  "description": "Search file contents by regular expression across the workspace. Returns matching lines with path and 1-based line number, best for finding where a symbol/string is used. Respects credential and vendored-tree exclusions. Prefer this over `bash grep` — it stays inside the workspace boundary, redacts credentials, and works even when network egress is contained.",
  "class": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "pattern": { "type": "string", "description": "Go (RE2) regular expression to match against each line." },
      "path": { "type": "string", "description": "Optional file or subdirectory (workspace-relative) to limit the search. Defaults to the whole workspace." },
      "glob": { "type": "string", "description": "Optional filename glob to restrict which files are searched by basename, e.g. \"*.go\" or \"*.{ts,tsx}\"." },
      "case_insensitive": { "type": "boolean", "description": "Case-insensitive matching (default false)." },
      "multiline": { "type": "boolean", "description": "Match across line boundaries (default false). With true, `.` matches newlines and the pattern is applied to the whole file, so a single match can span lines — e.g. \"func Foo\\\\([^)]*\\\\)[\\\\s\\\\S]*?\\\\n\\\\}\" to capture a function body. `^`/`$` still anchor at line boundaries; the reported line is the match's starting line." },
      "output_mode": { "type": "string", "enum": ["content", "files_with_matches", "count"], "description": "\"content\" (default) returns matching lines; \"files_with_matches\" returns only the paths that matched (cheaper); \"count\" returns per-file match counts." },
      "-A": { "type": "integer", "description": "Context: lines to include AFTER each match (like grep -A). content mode only. Capped at 20." },
      "-B": { "type": "integer", "description": "Context: lines to include BEFORE each match (like grep -B). content mode only. Capped at 20." },
      "-C": { "type": "integer", "description": "Context: lines to include both before AND after each match (like grep -C). Capped at 20." },
      "max_results": { "type": "integer", "description": "Maximum matching lines to return in content mode (default 100, cap 200)." }
    },
    "required": ["pattern"]
  }
}
