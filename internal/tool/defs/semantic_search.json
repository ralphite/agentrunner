{
  "name": "semantic_search",
  "description": "Search the workspace by meaning-ish relevance (identifier-aware lexical ranking). Returns the best-matching code/text chunks with path, line and snippet. Use for 'where is X handled' questions; use read_file to see full context.",
  "class": "read",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "description": "What to look for — natural language or identifiers." },
      "max_results": { "type": "integer", "description": "Maximum hits to return (default 8, cap 20)." }
    },
    "required": ["query"]
  }
}
